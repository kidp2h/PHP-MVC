{
  "name": "server-socket",
  "version": "1.0.0",
  "description": "server socket",
  "main": "socket.js",
  "author": "KidP2H",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "socket.io": "^4.4.1"
  },
  "scripts": {
    "start": "yarn build && nodemon index.ts",
    "build": "tsc --build"
  }
}
