{
  "require": {
   
  },
  "autoload": {
    "psr-4": {
      "app\\": "./app/",
      "core\\": "./core/",
      "utils\\": "./utils/",
      "database\\": "./database/",
      "tests\\": "./tests/"
    }
  },
  "scripts": {
    "start-docker": "docker compose up -d --build && docker exec php composer install",
    "stop": "docker compose down"
  },
  "require-dev": {
    "phpunit/phpunit": "^9.5"
  }
}
